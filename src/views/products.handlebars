<h1>Productos</h1>
{{#each products}}
  <div>
    <h2>{{this.title}}</h2>
    <p>{{this.description}}</p>
    <p>Precio: {{this.price}}</p>
    <a href="/carts/add/{{this._id}}">Agregar al carrito</a>
  </div>
{{/each}}
